@copilot The PR branch `copilot/fix-security-vulnerability-issues` is currently in a dirty/unmergeable state due to a merge conflict with `main`. Please:
1. Identify all files with merge conflicts between `main` and this PR branch
2. Resolve each conflict by preserving the security fixes from this PR branch
3. Merge `main` into `copilot/fix-security-vulnerability-issues` to bring it up to date
4. Push the resolved result so this PR becomes mergeable